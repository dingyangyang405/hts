  <td>{{category.id}}</td>
  <td>{{ dict_text('ticketPriority:html', category.priority) }}</td>
  <td>{{category.name}}</td>
  <td>{{category.delayedTime}}</td>
  <td>
    <button type="button" class="btn btn-sm btn-success js-update-btn" data-toggle="modal" data-target="#myModal" data-id = "{{category.id}}" data-url="{{ path('admin_category_update',{id:category.id}) }}">编辑</button>
    <button type="button" class="btn btn-sm btn-success js-delete-btn" data-id = "{{category.id}}" data-toggle="modal" data-target="#myModal" data-url="{{ path('admin_category_delete',{id:category.id})}}">删除</button>
  </td>